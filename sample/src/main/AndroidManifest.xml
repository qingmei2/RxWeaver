<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="qingmei2.github.qingmei2">

	<!-- To auto-complete the email text field in the login form with the user's emails -->
	<uses-permission android:name="android.permission.GET_ACCOUNTS" />
	<uses-permission android:name="android.permission.READ_PROFILE" />
	<uses-permission android:name="android.permission.READ_CONTACTS" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name="com.github.qingmei2.activity.MainActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity
			android:name="com.github.qingmei2.activity.login.LoginActivity"
			android:label="登录" />
		<activity
			android:name="com.github.qingmei2.activity.A03TokenExpiredActivity"
			android:label="Token失效异常处理" />
		<activity
			android:name="com.github.qingmei2.activity.A04MultiAsyncActivity"
			android:label="同时多个Token失效异常处理" />
	</application>

</manifest>